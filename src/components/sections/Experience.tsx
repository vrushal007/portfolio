// src/components/sections/Hero.tsx
import { Box, Typography } from '@mui/material';

export default function Experience() {
  return (
    <Box sx={{ 
      height: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
    }}>
      <Typography variant="h1">Experience Section</Typography>
    </Box>
  );
}
